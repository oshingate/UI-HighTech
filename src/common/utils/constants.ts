const ROOT_URL = 'https://localhost:4000/api';

const USER_URL = ROOT_URL + '/user';
const LOGIN_URL = ROOT_URL + '/users/login';
const REGISTER_URL = ROOT_URL + '/users/register';

export { LOGIN_URL, REGISTER_URL,ROOT_URL, USER_URL };
